version: "3.9"

services:
  preprocess-service:
    build: .
    container_name: preprocess-service
    ports:
      - "9000:9000"
    environment:
      - OCR_URL=http://172.17.0.1:${OLLAMA_PORT:-11434}/api/chat
      - OCR_MODEL=${OCR_MODEL:-maternion/LightOnOCR-2:1b}
      - OCR_TEMPERATURE=${OCR_TEMPERATURE:-0.2}
      - OCR_MAX_TOKENS=${OCR_MAX_TOKENS:-2048}
      - OCR_TOP_P=${OCR_TOP_P:-0.9}
      - CORS_ALLOW_ORIGINS=${CORS_ALLOW_ORIGINS:-*}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "3"
